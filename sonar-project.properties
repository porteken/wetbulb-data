sonar.projectKey=porteken_wetbulb-data
sonar.organization=porteken
sonar.qualitygate.wait=true

sonar.sources=.
sonar.tests=tests
sonar.test.inclusions=tests/**

sonar.exclusions=\
  .venv/**,\
  venv/**,\
  .conda/**,\
  __pycache__/**,\
  .ruff_cache/**,\
  .pytest_cache/**,\
  typings/**,\
  test_out/**,\
  tmp_*/**,\
  output_tiles/**,\
  pet_data_csv/**,\
  wetbulb_data_csv/**,\
  analytics_data_csv/**,\
  weather_data_parquet_test/**,\
  *.csv,\
  *.parquet,\
  *.log,\
  uv.lock

sonar.python.version=3.10
sonar.python.coverage.reportPaths=coverage.xml

sonar.coverage.exclusions=tests/**
sonar.cpd.exclusions=tests/**

# The project uses PostgreSQL, but SonarCloud treats .sql files as PL/SQL (Oracle) by default.
# This leads to warnings about missing Oracle Data Dictionary configuration for rules like S3641.
# We remap .sql away from PL/SQL to suppress these irrelevant warnings, as SQL linting is
# handled separately by sqruff in this project.
sonar.plsql.file.suffixes=plsql