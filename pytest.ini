[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    db: marks tests that require a live Postgres connection (auto-skipped when not set)

addopts = --cov=. --cov-report=xml --cov-fail-under=80
